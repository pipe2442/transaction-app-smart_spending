<section>
    <div class="user">
        <h1>Check Your Groups</h1>
        <p>In this section you can create new groups.</p>
    </div>
    <%= render partial: "form" %>
    <%= display_groups(current_user.groups.with_group.sort { |a, b| a.name <=> b.name }) %>
</section>